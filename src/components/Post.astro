---
const {post} = Astro.props;
---

<article>
    <a href={`/blog/${post.slug}`}>
        <img
            src={post.data.image}
            width={600}
            height={600 / 1.5}
            alt={post.data.title}
        >
    </a>
    <a href={`/blog/${post.slug}`}>
        <h2>{post.data.title}</h2>
    </a>
    <p class="line-clamp-2">
        {post.body}
    </p>
</article>